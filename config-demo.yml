
spring:
  profiles:
    active: dev

---
server:
  port: 8205
spring:
  profile: dev
  application:
    name: config-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/

---
server:
  port: 8206
spring:
  profile: test
  application:
    name: config-client

eureka:
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka/